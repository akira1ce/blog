---
title: BiomeJS configuration
category: ['configuration']
slug: configuration-biomejs
date: 2024-06-20
summary: 汇总 BiomeJS 的常用配置及插件推荐。
---

# install

```bash
yarn add -D @biomejs/biome

yarn exec biome --init
```

# config

## Biome.json

```js
// biome.json
{
  "$schema": "https://biomejs.dev/schemas/2.1.1/schema.json",
  "vcs": {
    "enabled": false,
    "clientKind": "git",
    "useIgnoreFile": false
  },
  "files": {
    "ignoreUnknown": false
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "tab"
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "nursery": {
        // 使用排序的类名
        "useSortedClasses": "error"
      },
      "suspicious": {
        // 禁用 noExplicitAny 规则
        "noExplicitAny": "off"
      },
      "correctness": {
        // 禁用 useExhaustiveDependencies 规则
        "useExhaustiveDependencies": "off"
      }
    },
    "domains": {
      "react": "recommended"
    }
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "double"
    }
  },
  "assist": {
    "enabled": true,
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  }
}
```

## .vscode/settings.json

配置 `editor.codeActionsOnSave` 来启用 Biome 的代码修复功能。

```js
{
  "editor.codeActionsOnSave": {
    "source.organizeImports.biome": "explicit",
    "source.fixAll.biome": "explicit",
    "quickfix.biome": "explicit"
  }
}
```
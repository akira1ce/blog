---
title: show
category:
  - Components
slug: comp-show
summary: React 中组件式条件渲染
createdTime: '2025-09-28 15:27:57'
updatedTime: '2025-11-12 15:59:45'
---

React 中组件式条件渲染，不再通过 js 表达式去做条件判断，代码组织结构更加清晰。

import CompShow from "@/demos/comp-show"
import Preview from '@/components/preview';
import { Tabs, TabItem } from '@/components/tabs';

<Preview>
  <CompShow />
</Preview>

<Tabs>
  <TabItem label="index.tsx">
    ```tsx
    'use client';

    import { useState } from 'react';
    import Show from './components/show';

    const Index = () => {
      const [show, setShow] = useState(false);

      return (
        <>
          <button className="cursor-pointer" onClick={() => setShow(!show)}>
            Click me!
          </button>
          <Show when={show}>
            <div>我来</div>
          </Show>
        </>
      );
    };

    export default Index;

    ```
  </TabItem>
  <TabItem label="components/show.tsx">
    ```tsx
    import { ReactNode } from 'react';

    export interface ShowProps {
      when: boolean;
      fallback?: ReactNode;
      children: ReactNode;
    }

    const Show = (props: ShowProps) => {
      const { when, fallback = null, children } = props;

      return when ? children : fallback;
    };

    export default Show;

    ```
  </TabItem>
</Tabs>

---
title: hook use update
category: ['react', 'hook']
slug: hook-use-update
date: 2025-06-16
summary: 强制更新
---

```ts
import { useState, useCallback } from 'react';

/**
 * Force update
 */
export const useUpdate = () => {
  const [_, update] = useState({});
  return useCallback(() => update({}), []);
};

```